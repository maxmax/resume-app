import { 
	githubApi, 
	useGetUserQuery, 
	useGetUserRepositoriesQuery, 
} from '../services/github';
import { 
	User, 
	Repository,
} from './types';
export { 
	githubApi,
	useGetUserQuery,
	useGetUserRepositoriesQuery,
};
export type { 
	User, 
	Repository,
};