import { githubApi, useGetUserQuery } from '../services/github';
import { User, Repository, Language } from './types';
export { 
	githubApi,
	useGetUserQuery
};
export type { 
	User, 
	Repository, 
	Language 
};